using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Basic3DMove : MonoBehaviour
{
    public Camera camera;
    float horizontal;
    float vertical;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        // horizontal = Input.MouseX();
        // vertical = Input.MouseY();


        // camera.rotate(EjeY, Horizontal, velocidadDeRotacion);
        // camera.rotacion(EjeX, Vertical, velocidadDeRotacion);

        // Codigo de movimiento normal

    }
}
